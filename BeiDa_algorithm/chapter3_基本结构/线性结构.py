"""
定义：
    线性结构是一种有序数据项的集合，其中每个数据项都有唯一的前驱和后继

特点：
    除了第一个没有前驱，最后一个没有后继
    新的数据项加入到数据集中时，只会加入到原有的某个数据项之前或之后

不同线性结构的区别：
    有的结构只允许数据项从一端添加，而有的结构则允许数据项从两端添加

四种结构：
    栈(Stack)：
    队列(Queue)：
    双端队列(Deque)：
    列表(List)：
"""